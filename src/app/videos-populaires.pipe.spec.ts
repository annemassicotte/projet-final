import { VideosPopulairesPipe } from './videos-populaires.pipe';

describe('VideosPopulairesPipe', () => {
  it('create an instance', () => {
    const pipe = new VideosPopulairesPipe();
    expect(pipe).toBeTruthy();
  });
});
