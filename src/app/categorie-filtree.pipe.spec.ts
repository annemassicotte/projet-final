import { CategorieFiltreePipe } from './categorie-filtree.pipe';

describe('CategorieFiltreePipe', () => {
  it('create an instance', () => {
    const pipe = new CategorieFiltreePipe();
    expect(pipe).toBeTruthy();
  });
});
